export const defaultValues = {
  OTP: '123456',
};

export const envConfig = {
  PRODUCTION: 'production',
};

export const REQUIRED_PROPERTY = { required: true };
export const OPTIONAL_PROPERTY = { required: false };
Object.freeze(REQUIRED_PROPERTY);
Object.freeze(OPTIONAL_PROPERTY);
